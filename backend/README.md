# Backend

Server-side components, daemons, RPC and CLI tools.

Mapping notes:
- Move `src/` non-UI binaries like `bitcoind`-style code into `backend/`.
- Keep configuration and init scripts under `backend/config`.