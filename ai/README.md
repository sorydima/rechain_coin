# AI

Machine learning components, model training code, and data processing pipelines.

Mapping notes:
- Add `ai/models`, `ai/training`, and `ai/utils` for future `ai_quantum_core` integration.
- Avoid heavy dependencies in the main build; keep these as optional components.