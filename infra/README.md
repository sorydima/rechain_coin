# Infra

CI/CD, packaging, deployment manifests, and platform build scripts.

Mapping notes:
- Put `contrib/` packaging scripts and OS-specific deployment files into `infra/`.
- Add CI config templates (`.github/workflows`, `gitlab-ci.yml`) and cross-compile toolchains here.