#include "rechain_core.h"
#include <stdio.h>

int rechain_core_init(void) {
    // placeholder init
    printf("rechain_core initialized (stub)\n");
    return 0;
}

void rechain_core_shutdown(void) {
    printf("rechain_core shutdown (stub)\n");
}
